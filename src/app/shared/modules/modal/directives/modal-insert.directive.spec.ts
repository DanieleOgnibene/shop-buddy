import { ModalInsertDirective } from "./modal-insert.directive";

describe("ModalInsert", () => {
  it("should create an instance", () => {
    const directive = new ModalInsertDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
